{
  "name": "codex-credit-stats",
  "version": "0.1.6",
  "description": "Estimate a weekly Codex credit limit from authenticated analytics responses and local Codex session rate-limit snapshots.",
  "type": "module",
  "main": "server.mjs",
  "scripts": {
    "cli": "node src/cli.mjs",
    "start": "node server.mjs",
    "start:web": "node server.mjs",
    "test": "node --test"
  },
  "bin": {
    "codex-credit-stats": "server.mjs",
    "codex-credit-stats-cli": "src/cli.mjs"
  },
  "files": [
    "server.mjs",
    "renderer/**/*",
    "src/**/*",
    "README.md",
    "README.zh-CN.md",
    "README.ja.md",
    "docs/**/*"
  ],
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "playwright": "^1.56.0"
  },
  "devDependencies": {}
}
